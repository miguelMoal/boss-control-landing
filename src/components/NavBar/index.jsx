import { Flex, Text } from "@/components";

const NavBar = () => {
  return (
    <Flex
      w="100vw"
      h="60px"
      style={{
        position: "fixed",
        top: 0,
        left: 0,
        background: "var(--bgNav)",
      }}
    >
      NavBar
    </Flex>
  );
};
export default NavBar;
